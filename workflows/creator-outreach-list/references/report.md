# Creator shortlist deliverable

Lead with the seed or content direction, chosen platform, filters, returned creator count and email coverage. Keep one row per creator:

| Creator | Platform / profile link | Similarity | Followers / average views | Region / language | Email | Contact source / status |
| --- | --- | --- | --- | --- | --- | --- |

Keep discovery order. Label contact source as discovery or email lookup, and status as returned, not found or lookup failed. Show the seed separately if it was researched. Preserve exact values in a requested JSON/CSV export; compact display counts are fine in prose. Missing metrics stay missing, never zero by assumption. Summarize API limitations and actual calls made. Do not dump raw payloads or send/upload the contact list unless requested.
